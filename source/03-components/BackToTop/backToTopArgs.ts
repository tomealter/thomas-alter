import { BackToTopProps } from '@/source/03-components/BackToTop/BackToTop';

const backToTopArgs = {
  topElement: 'top',
  text: 'Back to top',
  isDemo: true,
} satisfies BackToTopProps;

export default backToTopArgs;
