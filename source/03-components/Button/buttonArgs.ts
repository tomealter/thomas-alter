import { SharedButtonProps } from '@/source/03-components/Button/Button';

const buttonArgs = {
  label: 'Button',
  variant: 'primary',
  styleSize: 'medium',
} satisfies SharedButtonProps;

export default buttonArgs;
