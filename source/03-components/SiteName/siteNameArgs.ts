import { SiteNameProps } from '@/source/03-components/SiteName/SiteName';

const siteNameArgs = {
  siteName: 'Site Name',
  modifierClasses: '',
} satisfies SiteNameProps;

export default siteNameArgs;
