import { InputProps } from '@/source/03-components/FormItem/FormItem';

const telephoneArgs = {
  id: 'tel',
  label: 'Telephone',
  type: 'tel',
  size: 15,
} satisfies InputProps;

export default telephoneArgs;
