import { InputProps } from '@/source/03-components/FormItem/FormItem';

const monthArgs = {
  id: 'month',
  label: 'Month',
  type: 'month',
  size: 12,
} satisfies InputProps;

export default monthArgs;
