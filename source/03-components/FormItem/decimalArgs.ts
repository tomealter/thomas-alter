import { InputProps } from '@/source/03-components/FormItem/FormItem';

const decimalArgs = {
  id: 'decimal',
  label: 'Number (decimal)',
  type: 'number',
  placeholder: 'Placeholder',
  step: 0.01,
} satisfies InputProps;

export default decimalArgs;
