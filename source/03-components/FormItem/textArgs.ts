import { InputProps } from '@/source/03-components/FormItem/FormItem';

const textArgs = {
  id: 'text',
  label: 'Text',
  type: 'text',
  placeholder: 'Placeholder',
  size: 60,
  maxLength: 128,
} satisfies InputProps;

export default textArgs;
